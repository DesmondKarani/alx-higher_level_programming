//  a JavaScript script that updates the text color of the <header> element to red (#FF0000)
$(document).ready(function() {
  // Using jQuery to select colour
  $('header').css('color', '#FF0000');
});
